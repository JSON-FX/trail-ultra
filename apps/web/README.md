# apps/web — React + Vite

The **admin console** (super-admin platform console + org admin/editor/marshal).
The public per-org storefront (`/o/<slug>`) ships later in the same app. TypeScript.

**Not scaffolded yet.** Initialize with:

```bash
cd apps/web
pnpm create vite@latest . --template react-ts
```

Shares `@race-pace/shared` for types + validators.
